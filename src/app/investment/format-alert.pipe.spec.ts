import { FormatAlertPipe } from './format-alert.pipe';

describe('FormatAlertPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAlertPipe();
    expect(pipe).toBeTruthy();
  });
});
