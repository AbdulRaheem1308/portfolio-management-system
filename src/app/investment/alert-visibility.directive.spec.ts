import { AlertVisibilityDirective } from './alert-visibility.directive';

describe('AlertVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
